spring:
  datasource:
    url: jdbc:mysql://localhost:3309/ingrediant?serverTimezone=UTC
    username: example
    password: example
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

